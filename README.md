# Trail_for_github
Understanding about git hub 
<p>I am a student of LNCT college </p>
<br>
<h> Git Hub tutorial ..... </h>

<p>
cd ---- change directory 
to copy the the fine into local machine from git hub we use ---- git clone " the link " 
to get the list of file in the terminal --- ls 
to get all the files we use ---- ls -a ("gives hidden file ")

to check wether the data has been upto data between  local and git reposetry  ----- git status 

if we modify a file like we have created  a index.html file ---- so we will add and commit ----
U means ---- untracked file 
M means ----- modified file 

if we donot commit the file but have added a file than it becomes at the position called "  stage  " 

to add we use ----- git add 
to commit the file we use -----git commit -m " display the message " 

to finally push the file from local machine to github we use ---- git push origin main 


</p>